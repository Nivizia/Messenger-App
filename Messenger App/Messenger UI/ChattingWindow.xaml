<Window x:Class="Messenger_UI.ChattingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Messenger_UI"
        mc:Ignorable="d"
        Title="ChattingWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--Chatter username section-->
        <Label x:Name="ChattingUserName" Content="ChattingUserPlaceHolder" Grid.Row="0" Grid.Column="0"/>
        
        <!--Chat history section-->

        <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" BorderBrush="Black" BorderThickness="1" Margin="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ChatHistoryPanel">
                    <!-- Chat messages will be added here dynamically -->
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!--Message input section-->
        <TextBox x:Name="MessageInput" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Margin="10" Height="50" VerticalAlignment="Center"/>

    </Grid>
</Window>
